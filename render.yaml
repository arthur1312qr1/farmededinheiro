services:
  - type: web
    name: farmededinheiro-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:5000 --workers 1 --timeout 300 --keep-alive 2 --max-requests 1000 --preload main:app
    healthCheckPath: /health
    env: web
